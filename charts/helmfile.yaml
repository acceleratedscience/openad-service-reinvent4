# Example helmfile release.
# A release is autogenerated with the install wizard.

# releases:
  # - name: openad-model
  #   namespace: openad-models
  #   chart: ./helm
releases:
  - name: reinvent4-openad-model
    namespace: openad-models
    chart: ./reinvent4-openad-model
